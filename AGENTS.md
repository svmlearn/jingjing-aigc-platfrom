# AGENTS.md

本文件给进入 `小红书抖音矩阵获客平台` 工作区的 AI Agent 提供统一协作约定。

目标不是追求“自动化很炫”，而是降低上下文丢失、并行改动互相覆盖、以及最终交付对产品经理不友好的问题。

## 1. 先理解这个工作区

这是一个从 0 到 1 逐步展开的项目目录，目前仍处于“方案优先、实现随后”的阶段，不是已经成型的多仓工程。

当前已知核心文件：

- `AGENTS.md`：协作约定
- `docs/探索/2026-04-19-小红书抖音矩阵获客平台-初版方案.md`：当前架构、模块边界、MVP 路线的基线文档

推荐后续目录（出现后按此协作，不必一开始全部建齐）：

- `docs/handoff/`：交接文档、冻结说明、任务书
- `docs/progress/`：执行日志、验证记录、阶段结论
- `docs/探索/`：方案探索、竞品拆解、外部项目研究
- `docs/产品文档/`：PRD、信息架构、页面清单、业务规则
- `docs/架构规范/`：接口约束、状态机、发布链路规则、Prompt 规范
- `references/open-source/`：外部开源项目本地参考副本
- `apps/admin/` 或 `frontend/`：管理后台
- `services/api/` 或 `backend/`：后端 API
- `workers/publisher/`：发布执行 Worker、平台适配层

如果实际目录和这里不完全一致，以项目当前真实落地目录为准，但协作分层思路尽量保持一致。

## 2. 本项目默认工作方式

这不是“边聊边堆功能点”的项目。

更稳的方式是：

1. 先读上下文文档
2. 再确认当前任务边界
3. 规划类优先输出方案、结构、页面与流程
4. 实现类改完后补 handoff / progress
5. 默认先冻结成果，不直接把探索性结果并进主线

## 3. 启动顺序

进入项目后，优先按这个顺序获取上下文：

1. 先读本文件 `AGENTS.md`
2. 读 `docs/探索/2026-04-19-小红书抖音矩阵获客平台-初版方案.md`
3. 读 `docs/handoff/` 中最新的 `*zero-memory-handoff*.md`
4. 读当前任务对应的 `docs/handoff/*task*.md`
5. 读相关 `docs/progress/*.md`
6. 如任务涉及产品判断，再读 `docs/产品文档/`
7. 如任务涉及外部实现借鉴，再读 `references/open-source/README.md`

如果任务是探索类，不要一上来改代码，先看 `docs/探索/` 是否已有近似探索。

如果某些目录还没建立，跳过即可，不要因为文档里写了就假设它已经存在。

## 4. 真相源怎么分

不同类型的信息放在不同地方，不要混写。

### 4.1 `docs/handoff/`

这里放：

- 零上下文交接
- 当前任务书
- 协作协议
- 某个分支 / 工作树的冻结说明

### 4.2 `docs/progress/`

这里放：

- 执行日志
- 接口联调留痕
- 发布链路验证结果
- 阶段性共识
- 真正跑通和没跑通的事实

### 4.3 `docs/探索/`

这里放：

- 新功能探索
- 方案对比
- 外部仓库 / 方法拆解
- 页面草图、流程草图
- 还没进入正式实现的想法

### 4.4 `docs/产品文档/`

这里放：

- PRD
- 信息架构
- 页面清单
- 角色权限规则
- 商家、门店、账号相关业务规则

### 4.5 `docs/架构规范/`

这里放：

- 平台适配层 contract
- 发布任务状态机
- 内容数据结构
- Prompt 规范
- AI 工作流约束

如果某个能力已经形成稳定规则，不要长期只放在 `docs/探索/`。

探索稿先放 `docs/探索/`，一旦形成稳定共识，再搬到 `docs/架构规范/` 或 `docs/产品文档/`。

### 4.6 代码目录

代码事实以仓库本身为准，不要让文档替代代码。

文档负责帮助接手，不负责“伪造实现状态”。

## 5. Worktree 规则

这个项目后面很可能会进入“内容工作台 / API / 发布 Worker”并行推进阶段。

结论是：

1. `worktree` 对并行开发有效
2. 但对产品探索期项目来说，最后收口不能太重
3. 所以更适合“隔离开发 + 冻结交付 + 指定收口”，而不是所有人都自己复杂合并到底

### 5.1 什么时候开 worktree

满足以下任一条件，就优先开 worktree：

1. 改动不止一个文件
2. 会持续超过一轮会话
3. 主目录已有脏改动
4. 需要和另一位 Agent / 另一条任务并行
5. 涉及平台适配、发布链路、任务调度等中大型实现

### 5.2 什么时候不要直接在主目录改

不要直接在主目录里做中大型改动，尤其是：

1. 交互探索
2. 发布链路实验
3. 会多轮迭代的任务
4. 有可能被放弃的分支尝试

小规模文档修订、明确范围内的单文件改动，可以直接在当前目录完成。

### 5.3 本项目推荐的交付默认值

除非用户明确要求“现在就合并”，否则默认：

1. 在独立 worktree 完成改动
2. 跑最小验证
3. 产出最终 commit
4. 记录 patch 路径（如有）
5. 写 handoff
6. 状态记为“待验收 / 待合并决策”

不要默认帮用户把探索性分支合回主线。

### 5.4 合并策略

本项目默认不鼓励“谁开发谁自己复杂合并到底”。

更适合的方式是：

1. 开发者 worktree 负责把改动做干净
2. 集成人只在一个干净的集成工作区收口
3. 用户确认后再合并

如果必须整合多个分支，优先：

1. `fast-forward`
2. `cherry-pick`
3. 小范围明确 merge

尽量避免把用户拖进大冲突手工处理。

## 6. 已验证过的经验

这些不是理论，而是这类项目协作里最容易踩坑的地方。

### 6.1 先冻结基线，再开工

如果主仓有未提交改动，直接新开 worktree 可能拿不到这些改动。

所以开工前必须至少满足一个条件：

1. 当前基线已 commit
2. 或已经导出 patch
3. 或明确说明“本轮不继承主目录脏改动”

### 6.2 文件独占比“口头说不冲突”更靠谱

并行阶段要先划文件边界。

谁改哪些文件，要在任务书或日志里写明，不要靠记忆。

### 6.3 `docs/` 不是 worktree 自动隔离区

`docs/` 的协作靠纪律，不靠 Git 自动保护。

如果多人会改同一份文档：

1. 先在日志里写 `lock`
2. 写完后补 `unlock`

### 6.4 默认先“不 push / 不 merge”

这个项目前期会有很多探索型任务，先冻结结果、待验收，再决定要不要进主线，通常更适合用户。

### 6.5 部署不一定从带 `.git` 的目录做

如果部署平台、账号权限或 Git author 限制卡住了，不要死磕一种方式。

可考虑：

1. 用干净快照目录部署
2. 明确目标环境，不要误发到错误项目
3. 先做测试环境验证，再做正式环境动作

### 6.6 发布目标不要搞错

这类项目最危险的错误之一，不是“代码没跑”，而是“内容发对了，账号发错了”。

涉及真实发布时，先确认：

- 目标商家
- 目标门店
- 目标平台
- 目标账号
- 测试环境还是正式环境

## 7. Handoff 规则

本项目已经默认采用：`handoff` 比依赖聊天记录更可靠。

### 7.1 什么情况下必须写 handoff

以下情况结束前必须留 handoff：

1. 当前任务没做完
2. 已做完但暂不合并
3. 下一位 Agent 需要接手
4. 结果需要用户验收后再决定

### 7.2 handoff 最少包含什么

至少写清楚：

1. 当前目标
2. 已完成内容
3. 正在做什么
4. 下一步建议
5. 改动文件
6. 对应 branch / worktree
7. 最终 commit
8. 验证结果
9. 是否 `push / merge`

### 7.3 handoff 放哪

默认放在：

- `docs/handoff/`

文件名建议：

- `YYYY-MM-DD-<task>-handoff.md`
- `YYYY-MM-DD-<topic>-zero-memory-handoff.md`

## 8. Progress 规则

如果这轮工作是“执行了什么、修了什么、验证了什么”，优先写到：

- `docs/progress/`

尤其是：

1. 数据结构调整记录
2. 接口联调记录
3. 发布验证结果
4. 回滚点
5. 关键命令与输出结论
6. 账号授权与失效情况

## 9. 任务默认模板

如果要开始一个正式实现任务，建议先明确这几项：

1. 任务目标
2. 涉及目录
3. 不要碰的文件
4. 是否需要 worktree
5. 交付形式：
   - 只出探索文档
   - 代码 + handoff
   - 代码 + 验证 + 待合并
   - 直接合并

如果用户没说清，默认按：

`代码 + 验证 + handoff + 不直接合并`

## 10. 当前推荐的 AGENTS 布局

当前阶段，**优先使用根目录这一份 `AGENTS.md`**。

原因：

1. 这个项目目前还是单项目根目录，不是复杂多仓
2. 当前最关键的问题是“从方案到实现如何不断层”
3. 目前最重要的是跨文档、跨模块的接手与收口，不是某个子目录内部规范

所以：

1. `AGENTS.md` 放根目录最合适
2. 先不要一上来拆很多层级的 `AGENTS.md`

只有在以后出现这种情况，再考虑加子目录 `AGENTS.md`：

1. `apps/admin/` 有非常独立且稳定的前端规则
2. `services/api/` 有完全不同的服务端约束
3. `workers/publisher/` 已复杂到需要自己的局部启动顺序

## 11. 对 Agent 的行为要求

进入本项目工作时：

1. 不要假设“采集、改写、发布”已经形成完整闭环
2. 不要把产品设想写成仿佛已经实现的状态
3. 不要在未确认范围时大面积改动
4. 不要忽略已有 handoff / progress
5. 不要把一次性聊天结论当长期真相源
6. 要优先给用户提供“可接手、可验收、可回退”的结果
7. 涉及真实账号和真实发布时，默认更保守

一句话说：

在 `小红书抖音矩阵获客平台`，Agent 的价值不是“多写一点代码”，而是**把探索、实现、交接、收口都做得让产品经理和后续执行者接得住**。
