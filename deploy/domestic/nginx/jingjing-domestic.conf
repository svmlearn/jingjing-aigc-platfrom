# IP-stage domestic verification only.
# Copy to /etc/nginx/conf.d/jingjing-domestic.conf and run: nginx -t && systemctl reload nginx

upstream jingjing_app {
    server 127.0.0.1:3000;
    keepalive 32;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 1024m;
    access_log /var/log/nginx/jingjing-access.log;
    error_log /var/log/nginx/jingjing-error.log warn;

    location / {
        proxy_pass http://jingjing_app;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    location = /nginx-health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }
}
