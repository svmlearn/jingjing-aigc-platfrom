# 2026-04-20 V0.6 B 分支 Admin UI / Rewrite UX Handoff

## 1. 当前目标

分支：

```text
feature/b-admin-ui-rewrite
```

Worktree：

```text
/Users/wy/.codex/worktrees/bf3b/小红书抖音矩阵获客平台
```

本轮目标是完成 B 分支可操作后台体验：

```text
注册页
-> 商户资料页
-> 导入页
-> 内容中心
-> 内容详情 / 评论
-> 改写工作台
-> 草稿体验
```

## 2. 已完成内容

### 2.1 路由与页面

已完成：

- `/`：直接进入 `/dashboard/import`
- `/register`：邀请码注册演示
- `/merchant/onboarding`：商户资料补全
- `/dashboard/import`：导入页
- `/dashboard/content`：内容中心
- `/dashboard/content/[sourceItemId]`：内容详情页
- `/dashboard/rewrite/[sourceItemId]`：改写工作台
- `/dashboard/drafts/[draftId]`：草稿详情
- `/dashboard/merchant-profile`：商户资料编辑

### 2.2 前端体验

已完成：

- 后台全局壳与侧边导航
- 注册表单、邀请码成功 / 失败提示
- 商户资料 5 字段表单
- 小红书 / 抖音链接导入表单
- 最近导入任务列表与状态文案
- 内容中心导入内容 / 改写草稿双入口
- 内容详情抽屉式区域
- 评论洞察和评论列表
- 低质量导入提示
- 改写设置、评论摘要、生成结果
- 保存为草稿前端状态
- 草稿详情继续编辑与保存状态

### 2.3 Mock Adapter

新增：

```text
app/src/lib/ui/mock-api.ts
app/src/lib/ui/format.ts
```

当前 mock 数据使用现有共享 contract：

- `app/src/contracts/import.ts`
- `app/src/contracts/content.ts`
- `app/src/contracts/draft.ts`

B 分支没有直接调用：

- Apify
- OpenAI
- Supabase service role

## 3. 改动文件

主要新增：

```text
app/src/app/(auth)/register/page.tsx
app/src/app/(auth)/merchant/onboarding/page.tsx
app/src/app/dashboard/layout.tsx
app/src/app/dashboard/import/page.tsx
app/src/app/dashboard/content/page.tsx
app/src/app/dashboard/content/[sourceItemId]/page.tsx
app/src/app/dashboard/rewrite/[sourceItemId]/page.tsx
app/src/app/dashboard/drafts/[draftId]/page.tsx
app/src/app/dashboard/merchant-profile/page.tsx
app/src/components/app/dashboard-shell.tsx
app/src/components/app/page-header.tsx
app/src/components/dashboard/registration-flow.tsx
app/src/components/dashboard/merchant-profile-form.tsx
app/src/components/dashboard/import-console.tsx
app/src/components/dashboard/content-center.tsx
app/src/components/dashboard/content-detail.tsx
app/src/components/dashboard/content-detail-page.tsx
app/src/components/dashboard/rewrite-workbench.tsx
app/src/components/dashboard/draft-detail.tsx
app/src/lib/ui/mock-api.ts
app/src/lib/ui/format.ts
```

主要修改：

```text
app/src/app/page.tsx
app/src/app/globals.css
app/src/components/ui/button.tsx
app/src/components/ui/card.tsx
```

说明：

- `button.tsx` 增加 `asChild`，用于 Link / a 包裹按钮样式。
- `card.tsx` 和 `globals.css` 收敛圆角，保证卡片 / 按钮不超过 8px。
- mock 缩略图使用 `background-image`，避免 B 分支改 Next image 配置。

## 4. 验证结果

在 `app/` 目录已执行：

```bash
pnpm lint
pnpm build
```

结果：

- `pnpm lint`：通过，无 warning
- `pnpm build`：通过

Build 路由结果包含：

```text
/dashboard/import
/dashboard/content
/dashboard/content/[sourceItemId]
/dashboard/rewrite/[sourceItemId]
/dashboard/drafts/[draftId]
/dashboard/merchant-profile
/merchant/onboarding
/register
```

## 5. 当前状态

已完成：

- B 分支代码实现
- lint / build 验证
- handoff 记录

正在做：

- 等待用户验收

最终 commit：

```text
25ec6ee feat: build admin UI rewrite mock flow
```

Push / merge：

```text
未 push
未 merge
```

## 6. 下一步建议

1. 用户在本 worktree 里手动验收页面流程。
2. A 分支完成真实 API 后，用真实 API adapter 替换 `app/src/lib/ui/mock-api.ts`。
3. 回到 `main` 后建议先合 A，再合 B，最后做 API contract 对接修正。

## 7. 注意事项

- 本分支没有新增真实 API route。
- 本分支没有写入任何真实 token。
- 当前 `docs/handoff/2026-04-20-v0.6-new-session-worktree-start.md` 不在 B 分支基线里，本轮是从 main 目录读取该启动说明后继续执行。
