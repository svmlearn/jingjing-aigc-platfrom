# 2026-04-20 V0.6 新 Session / Worktree 启动说明

## 1. 给新 session 的第一句话

请先确认当前工作目录是项目 Git 仓库根目录：

```text
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台
```

不是：

```text
/Users/wy/Desktop/静境/静境4.0
```

如果在上层目录启动，Codex 的“派生到新工作树”会报：

```text
Not a git repository
```

原因是 `.git` 在 `小红书抖音矩阵获客平台/` 目录里。

---

## 2. 当前 Git 基线

当前已经完成两个基线 commit：

```text
48a30b9 docs: initialize planning baseline
abe5731 chore: scaffold next app baseline
```

当前 `main` 应该停在：

```text
abe5731
```

当前已创建两个分支指针：

```text
feature/a-core-import
feature/b-admin-ui-rewrite
```

这两个分支都应该从 `abe5731` 出发。

---

## 3. 切换后先做的检查

新 session 进入项目根目录后，先执行：

```bash
git status --short --branch
git log --oneline --decorate -3
git branch --list
```

预期：

```text
## main
```

并看到：

```text
feature/a-core-import
feature/b-admin-ui-rewrite
main
```

如果工作区不干净，先不要开工，先确认脏改动来源。

---

## 4. 派生 Worktree 的建议方式

如果用 Codex UI：

1. 先把 workspace 切到项目根目录
2. 再点“派生到新工作树”
3. 分别基于下面两个分支派生：
   - `feature/a-core-import`
   - `feature/b-admin-ui-rewrite`

如果用命令手动创建：

```bash
git worktree add ../小红书抖音矩阵获客平台-a-core feature/a-core-import
git worktree add ../小红书抖音矩阵获客平台-b-ui feature/b-admin-ui-rewrite
```

建议最终得到：

```text
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台-a-core
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台-b-ui
```

---

## 5. 主分支 `main` 的职责

`main` 暂时只做：

- 保留共同基线
- 最后集成 A/B 分支
- 做端到端验收
- 修少量整合问题

`main` 暂时不要直接做大功能。

如果要动 `main`，优先只做：

- 文档更新
- 小范围修正
- 合并后的集成修复

---

## 6. A 分支任务：`feature/a-core-import`

### 6.1 目标

把后端核心链路跑通：

```text
邀请码注册
-> 商户资料
-> Apify 导入
-> source_items / imported_comments 落库
```

### 6.2 主要负责

- Supabase migration 接入
- 邀请码注册 API
- 商户资料 API
- `ApifyProviderAdapter`
- `ImportJob` 状态机
- 小红书/抖音导入 API
- 结果质量校验
- 导入限流
- 错误处理

### 6.3 主要文件范围

```text
app/src/server/**
app/src/lib/supabase/**
app/src/lib/db/**
app/src/lib/auth/**
app/src/app/api/**
app/supabase/**
app/.env.example
```

### 6.4 不要做

- 不做复杂 UI
- 不做前端直连 Apify
- 不把任何 token 写进代码
- 不改 B 分支负责的页面体验，除非是 API contract 必须调整

### 6.5 A 分支完成标准

- `pnpm lint` 通过
- `pnpm build` 通过
- 可以创建邀请码
- 可以注册商户 owner
- 可以更新商户资料
- 可以导入小红书 detail
- 可以导入小红书 comments
- 可以导入抖音 detail/comments
- 可以写入 `source_items`
- 可以写入 `imported_comments`
- 服务端密钥不进入客户端 bundle

---

## 7. B 分支任务：`feature/b-admin-ui-rewrite`

### 7.1 目标

把用户能操作的后台体验做出来：

```text
注册页
-> 商户资料页
-> 导入页
-> 内容中心
-> 改写工作台
-> 草稿体验
```

### 7.2 主要负责

- 后台全局壳
- 注册页 UI
- 商户资料页 UI
- 导入页 UI
- 内容中心 UI
- 内容详情抽屉
- 评论列表 UI
- 改写工作台 UI
- 草稿列表和详情
- AI 改写交互体验

### 7.3 主要文件范围

```text
app/src/app/(auth)/**
app/src/app/dashboard/**
app/src/components/**
app/src/styles/**
app/src/lib/ui/**
```

### 7.4 可以用 mock

B 分支可以先使用 mock 数据和 mock API adapter。

但必须遵守：

- 使用 `app/src/contracts/**`
- 不直接调用 Apify
- 不直接调用 OpenAI
- 不直接使用 Supabase service role

### 7.5 B 分支完成标准

- `pnpm lint` 通过
- `pnpm build` 通过
- 能走注册/商户资料 UI 流程
- 能粘贴链接发起导入请求
- 能查看内容列表
- 能打开内容详情
- 能查看评论列表
- 能进入改写工作台
- 能保存草稿的前端流程跑通

---

## 8. A/B 分支共同约束

### 8.1 共享 contract

优先复用：

```text
app/src/contracts/import.ts
app/src/contracts/content.ts
app/src/contracts/draft.ts
```

不要在 A/B 分支各自重新定义一套 DTO。

### 8.2 密钥边界

只能服务端读取：

```text
SUPABASE_SERVICE_ROLE_KEY
APIFY_TOKEN
OPENAI_API_KEY
```

浏览器只能使用：

```text
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

### 8.3 API 边界

前端只能调用我们自己的 API：

```text
POST /api/import-jobs
GET  /api/import-jobs/:id
GET  /api/source-items
GET  /api/source-items/:id
GET  /api/source-items/:id/comments
POST /api/source-items/:id/rewrite
GET  /api/content-drafts
PATCH /api/content-drafts/:id
```

---

## 9. 合并回 `main` 的建议顺序

建议：

1. A 分支先完成并提交
2. B 分支完成并提交
3. 回到 `main`
4. 先合并 A
5. 再合并 B
6. 解决 UI 接真实 API 的整合问题
7. 在 `main` 跑：

```bash
cd app
pnpm lint
pnpm build
```

如果接了真实 Supabase 和 Apify，再补端到端手测：

- 注册
- 商户资料
- 导入小红书 detail
- 导入小红书 comments
- 内容中心查看
- 进入改写
- 保存草稿

---

## 10. 新 session 必读文档

按顺序读：

1. [AGENTS.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/AGENTS.md)
2. [2026-04-20-v0.4-architecture-ab-branch-handoff.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/handoff/2026-04-20-v0.4-architecture-ab-branch-handoff.md)
3. [2026-04-20-v0.5-app-scaffold-baseline.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/handoff/2026-04-20-v0.5-app-scaffold-baseline.md)
4. 本文件

如果做 A 分支，再读：

- [V0.1-ApifyProviderAdapter-接口设计草案.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/架构规范/V0.1-ApifyProviderAdapter-接口设计草案.md)
- [V0.1-A-Supabase-Migration-草案.sql](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/架构规范/V0.1-A-Supabase-Migration-草案.sql)

如果做 B 分支，再读：

- [V0.1-A-导入到改写页面流程草案.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/产品文档/V0.1-A-导入到改写页面流程草案.md)
- [V0.1-A-实现任务拆解.md](/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台/docs/产品文档/V0.1-A-实现任务拆解.md)

---

## 11. 给用户的操作提醒

如果要用 Codex UI 派生：

1. 新开 session 时选中这个目录：

```text
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台
```

2. 不要选上层：

```text
/Users/wy/Desktop/静境/静境4.0
```

3. 派生失败时，先让 Agent 执行：

```bash
pwd
git status --short --branch
git rev-parse --show-toplevel
```

如果 `git rev-parse` 不是项目目录，就说明 workspace 选错了。
