# V2.2 Admin Foundation Handoff

日期：2026-04-27
Branch：`codex/v2.2-admin-foundation`
状态：待验收 / 待决定是否 apply staging / 不 merge main

## 当前目标

完成 V2.2 后台 Agent 控制台 foundation：

1. 数据库 foundation migration。
2. shared contracts。
3. repository 读取骨架。
4. 初始化 seed。

明确不做：

- 不改后台 UI。
- 不改 `platform-admin-login`。
- 不接 `consultation-service` runtime。
- 不实现 Agent 调试台真实运行。
- 不做完整积分扣费逻辑。

## 本轮改动文件

```text
app/supabase/migrations/202604270001_v22_agent_console_foundation.sql
app/src/contracts/agent-console.ts
app/src/contracts/platform-admin.ts
app/src/lib/db/agent-console-repository.ts
docs/progress/2026-04-27-v2.2-agent-console-foundation.md
docs/handoff/2026-04-27-v2.2-admin-foundation-handoff.md
```

## 已完成内容

### Migration

新增 V2.2 foundation migration，包含：

- `platform_admin_users`
- `agent_configs`
- `agent_prompt_versions`
- `agent_skills`
- `agent_skill_bindings`
- `knowledge_sets`
- `knowledge_set_documents`
- `agent_knowledge_set_bindings`
- `agent_route_bindings`
- `agent_test_runs`
- `agent_runtime_snapshots`
- `merchant_memberships`
- `merchant_credit_accounts`
- `merchant_usage_events`
- `merchant_credit_ledger`

### Seed

migration 内置初始化逻辑：

- `base_platform_knowledge`
- `initial_consultation_agent`
- 从旧 `platform_settings.consultation_agent.systemPrompt` 迁移 active prompt
- `initial_consultation_agent -> base_platform_knowledge`
- `consultation_default -> initial_consultation_agent`

如果已有有效 `consultation_default` 绑定，不重复创建初始 Agent。

### Contracts

新增 `agent-console.ts`，并从 `platform-admin.ts` re-export。

### Repository

新增 `agent-console-repository.ts`，提供 foundation 读取函数和 row -> DTO mapper。

## 验证结果

在 `app/` 下执行：

```text
pnpm install
pnpm lint
pnpm build
```

结果：

- `pnpm install`：通过；用于补齐当前 worktree 缺失的 `node_modules`。
- `pnpm lint`：通过。
- `pnpm build`：通过。

## Staging / Push / Merge

- Staging Supabase：未 apply。
- Push：未 push。
- Merge main：未 merge。

建议用户验收 migration SQL 后，再决定是否 apply 到 staging。apply 后需要补 progress 状态。

## 注意事项

1. 当前 worktree 进入时已有一批 unrelated 文档 / design 目录变动，本轮没有回滚，也不会纳入 foundation commit。
2. `agent-console-repository.ts` 只做读取骨架，不做写操作。
3. 会员 / 积分表只是 foundation 预留，不代表 V2.2 已经实现真实扣费。
4. RLS 已启用；后续后台 API 仍应从 server/service role 侧访问。
5. `main` 作为最终验收分支时，建议先合入并冻结本 foundation，再从该基线开后续功能分支。

## 后续分支接入顺序

推荐：

1. `codex/v2.2-admin-auth-rbac`
2. `codex/v2.2-agent-assets-api`
3. `codex/v2.2-admin-console-ui`
4. `codex/v2.2-agent-runtime-debug`

其中 runtime/debug 分支最好等 Agent Assets API 的写操作和线上绑定能力稳定后再真实接商家端咨询。
