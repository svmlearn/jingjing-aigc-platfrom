# 2026-05-04 V2.2 Agent Console 能力资产管理接通记录

## 背景

用户要求阅读：

- `docs/产品文档/V2.2-咨询Agent控制台与能力资产管理PRD.md`
- `docs/架构规范/2026-05-03-consultation-agent-runtime-modularization-design.md`

对齐后确认：runtime 模块化已基本完成，当前更明显的缺口在平台管理台的能力资产管理界面。已有后端 API，但前端仍有多处只读/等待接入文案。

## 本轮完成

### 1. Skill 管理从只读接入真实 CRUD

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`

完成：

- `技能管理` 支持新建 Skill。
- 支持编辑：
  - 名称
  - description
  - when_to_use
  - body
  - status
  - dependencies
- 支持启用 / 禁用。
- 保存后使用 `/api/platform-admin/skills` 与 `/api/platform-admin/skills/[skillId]`。
- 禁用 Skill 后沿用 repository 现有逻辑，禁用其 enabled bindings。

### 2. System Prompt 草稿 / 发布接入

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`
- `app/src/lib/db/agent-console-repository.ts`

完成：

- Agent 配置页的 System Prompt 草稿可编辑。
- 支持保存草稿，不影响当前 active prompt。
- 支持发布草稿，发布后下一轮真实咨询读取新的 active prompt。
- 后端增加保护：
  - Agent 启用前必须有 active System Prompt。
  - 设为 `consultation_default` 前必须有 active System Prompt。
  - 当前默认 Agent 不允许直接下线 / 切到非 enabled 状态，需先切换默认 Agent。

### 3. Knowledge Set 创建与上传归属选择

文件：

- `app/src/components/platform-admin/platform-knowledge-manager.tsx`
- `app/src/app/platform-admin/knowledge/page.tsx`
- `app/src/app/api/platform-admin/knowledge/documents/route.ts`

完成：

- 知识管理左侧支持新建 Knowledge Set。
- 页面读取 `knowledge_set_documents` 关系，用于按知识集筛选文档。
- 文档卡片展示已加入的 Knowledge Set。
- 上传平台方法论时必须至少选择一个 Knowledge Set。
- 上传接口接收 `knowledgeSetIds`，文档入库后写入 `knowledge_set_documents`。
- 未选择知识集时返回 `KNOWLEDGE_SET_REQUIRED`。

### 4. 回归测试补充

文件：

- `app/src/server/api/agent-console-admin.test.ts`

新增断言：

- Agent Console 支持 Skill 新建、保存、启停。
- Knowledge Manager 上传时要求知识集归属。
- Prompt 草稿/发布存在，并且上线前 active prompt gate 生效。

## 追加完成：PRD 剩余验收项收口

### 5. Agent Knowledge Set 挂载保存

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`

完成：

- Agent 配置页的 Knowledge Set 挂载从只读展示改为可勾选。
- 仅 enabled Knowledge Set 可保存为 enabled binding。
- 保存后调用 `/api/platform-admin/agents/[agentId]/knowledge-sets`。
- 未挂载任何知识集时继续提示「Knowledge 服务不会检索平台知识」。

### 6. Prompt 历史版本回滚

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`

完成：

- Prompt 历史版本列表支持对 archived 版本执行回滚。
- 回滚后切换为 active，原 active 版本归档。

### 7. Knowledge Set 编辑 / 启停

文件：

- `app/src/components/platform-admin/platform-knowledge-manager.tsx`

完成：

- 知识集详情支持编辑名称、描述和状态。
- 支持启用 / 禁用 Knowledge Set。
- 禁用后沿用 repository 现有逻辑，将相关 Agent enabled bindings 置为 disabled。

### 8. Agent 调试台真实运行

文件：

- `app/src/app/platform-admin/debug/page.tsx`
- `app/src/app/api/platform-admin/agents/test-runs/route.ts`
- `app/src/components/platform-admin/agent-console-pages.tsx`
- `app/src/server/api/consultation-service.ts`
- `app/src/lib/db/agent-console-repository.ts`

完成：

- `/platform-admin/debug` 不再是禁用占位。
- 管理员可选择任意 Agent 和测试商家，输入测试问题并运行。
- 调试运行复用咨询 runtime，但不写真实 `consultation_sessions` / `consultation_events`。
- 调试结果保存到 `agent_test_runs`。
- 调试台展示最终回复、工具摘要、知识命中和当前配置快照。
- 调试运行不扣商家积分。

### 9. 会员 / 积分 gate 与用量事件预留

文件：

- `app/src/server/api/consultation-service.ts`
- `app/src/lib/db/agent-console-repository.ts`

完成：

- 商家端真实咨询调用前执行 `checkConsultationEntitlement`。
- 自动确保 `merchant_credit_accounts` 存在。
- 首次创建账户时按当前 plan 写入初始 grant ledger。
- 积分不足时阻止本次 AI 调用，并返回「当前积分不足，无法继续使用该 AI 能力。请升级会员或补充积分。」
- 成功咨询后写入 `merchant_usage_events`，并写入 `merchant_credit_ledger` 消耗流水。
- Supabase admin 未配置时降级为 skipped usage event，不阻断本地 demo。

### 10. Runtime Knowledge Set 状态过滤

文件：

- `app/src/server/api/consultation-runtime/experts.ts`

完成：

- Agent 运行时只把 enabled Knowledge Set 展开为平台 knowledge document scope。
- disabled Knowledge Set 即使 binding 仍存在，也不会进入平台知识检索范围。

### 11. 商家端默认 Agent 异常保护

文件：

- `app/src/server/api/consultation-service.ts`

完成：

- 商家端真实咨询在扣积分和写用户消息前校验本轮是否有可用 Agent 容器。
- 如果没有默认 Agent，但存在 enabled Agent，返回「请选择一个专家开始咨询。」
- 如果没有任何 enabled Agent，返回「咨询服务暂未配置，请联系平台管理员。」
- 避免默认入口异常时静默退回旧 `platform_settings.consultation_agent` 继续运行。

## 验证结果

已通过：

```bash
cd app && node --test src/server/api/agent-console-admin.test.ts src/server/api/consultation-service.test.ts
cd app && npm run typecheck
cd app && npm run lint -- src/components/platform-admin/agent-console-pages.tsx src/components/platform-admin/platform-knowledge-manager.tsx src/app/platform-admin/knowledge/page.tsx src/app/platform-admin/debug/page.tsx src/app/api/platform-admin/knowledge/documents/route.ts src/app/api/platform-admin/agents/test-runs/route.ts src/lib/db/agent-console-repository.ts src/server/api/agent-console-admin.test.ts src/server/api/consultation-service.ts src/server/api/consultation-runtime/experts.ts src/server/api/schemas.ts
cd app && npm run build
```

结果：

- 31 条相关测试通过。
- TypeScript typecheck 通过。
- targeted ESLint 通过。
- Next.js production build 通过，50 个页面生成完成。
- `node --test` 仍有既有 package ESM warning，不影响本轮验证。

## 未做

- 未新增 Supabase migration。
- 未部署 Vercel。
- 未 commit / push / merge。
- 未完成 long-task-gate 独立 verifier：
  - 硬门禁已全部通过。
  - `codex exec` verifier 被外部 Codex CLI usage limit 阻塞，提示 2026-05-06 20:30 后可重试。
  - 因 Completion Gate 规则要求 verifier 通过后才可写入 complete，本轮只记录为外部阻塞，不输出 completion promise。

## 风险与注意

- 当前本地 `.env.local` 据上一份 handoff 记录已被拉成 Production 环境变量。本轮没有进行线上写操作和部署。
- 新增的 Knowledge 上传归属写入是“先创建 document，再写 membership”。正常 UI 传入已有 knowledge set 时可用；若未来要进一步增强，需要在上传前先做 set id 预校验。
- 会员 / 积分 gate 已完成运行时拦截、usage event 和 ledger 预留；真实付费购买、账单周期重置、运营补点仍属于后续商业化闭环。

## 追加完成：2026-05-04 PRD 缺口补齐验收

本轮根据人工复扫出的 8 个缺口继续收口，目标是把 `V2.2-咨询Agent控制台与能力资产管理PRD.md` 和 runtime 模块化设计里的剩余验收点补到可验收状态。

### 12. Knowledge Set 空挂载检索边界

文件：

- `app/src/lib/db/knowledge-repository.ts`

完成：

- `documentIds === undefined` 继续代表旧链路“允许全平台知识 + 商家私有知识”。
- `documentIds: []` 明确代表 Agent 未挂载平台 Knowledge Set，只允许当前商家的私有 indexed knowledge / memory，不再误召回所有平台知识。
- 该修复对应 PRD 中“Agent 未挂载 Knowledge Set 时不检索平台知识”的验收点。

### 13. Skill 依赖未满足提示

文件：

- `app/src/server/api/consultation-runtime/types.ts`
- `app/src/server/api/consultation-runtime/skills.ts`
- `app/src/server/api/consultation-runtime/events.ts`
- `app/src/server/api/consultation-runtime/runtime.ts`
- `app/src/server/api/consultation-service.ts`
- `app/src/components/platform-admin/agent-console-pages.tsx`

完成：

- Runtime Skill 保留 `dependencies`。
- 新增 `buildSkillDependencyWarnings()`，识别 `knowledge / knowledge_retrieval / retrieve_knowledge_base` 依赖。
- Agent 配置页在挂载 Skill 后，如果依赖 Knowledge 但 Agent 未启用 Knowledge 或未挂载可用知识集，会展示风险提示。
- Agent 调试台运行结果也返回并展示依赖未满足提示。
- Runtime event 和 runtime snapshot 的 `toolCallSummary` 中记录 `skillDependencyWarnings`。

### 14. Agent 调试台可观测增强

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`
- `app/src/server/api/consultation-service.ts`

完成：

- 调试台除了最终回复、工具摘要和知识命中外，新增展示：
  - 实际加载 Skills
  - Skill 触发分数与 triggerReasons
  - Memory 调用情况
  - Knowledge 服务状态
  - Skill 依赖未满足提示

### 15. Memory 快照记录

文件：

- `app/src/server/api/consultation-runtime/runtime.ts`
- `app/src/server/api/consultation-service.ts`

完成：

- Runtime snapshot 不再固定 `memoryMatchIds: []`。
- 当知识命中片段来自 `metadata.contentKind = merchant_memory` 时，记录到 `memoryMatchIds`。
- `toolCallSummary.memoryMatches` 记录 memory 命中的 chunk、document 和标题，便于测试台和后续排查。

### 16. 积分 gate 预留升级为 reservation 形态

文件：

- `app/src/lib/db/agent-console-repository.ts`
- `app/src/server/api/consultation-service.ts`

完成：

- 真实咨询调用前先写入 `merchant_usage_events.status = reserved`。
- 咨询成功后把 reserved usage event 更新为 `consumed`，再写积分消耗 ledger。
- 如果消耗链路异常，额外写入 `failed / usage_compensation_required` 用量事件，避免静默丢失。
- 保留 Supabase admin 未配置时的本地 demo 降级路径。

### 17. 知识入库失败原因展示

文件：

- `app/src/components/platform-admin/platform-knowledge-manager.tsx`

完成：

- 知识管理列表在最新 ingestion job 为 `failed` 且存在 `errorSummary` 时，展示明确失败原因。
- 保留“重跑入库”操作。

### 18. 后台默认入口异常提示

文件：

- `app/src/components/platform-admin/agent-console-pages.tsx`

完成：

- Agent 配置页如果发现 `consultation_default` 指向的 Agent 不存在，展示「默认 Agent 未正确配置」提示。
- 商家端已有的未配置 / 选择专家保护保持不变。

### 19. 新建 enabled Agent 的 Prompt gate

文件：

- `app/src/lib/db/agent-console-repository.ts`

完成：

- `createAgentConfig()` 不再允许直接创建 `serviceStatus = enabled` 的 Agent。
- 新 Agent 必须先创建草稿、保存/发布 System Prompt，再通过更新或“设为线上”进入 enabled/default。

## 追加验证结果

已通过：

```bash
cd app && node --test src/server/api/agent-console-admin.test.ts src/server/api/consultation-service.test.ts
cd app && npm run typecheck
cd app && npm run lint -- src/server/api/consultation-runtime src/server/api/consultation-service.ts src/server/api/consultation-service.test.ts src/server/api/agent-console-admin.test.ts src/lib/db/agent-console-repository.ts src/lib/db/knowledge-repository.ts src/components/platform-admin/agent-console-pages.tsx src/components/platform-admin/platform-knowledge-manager.tsx
cd app && npm run build
git diff --check -- app/src/server/api/consultation-runtime app/src/server/api/consultation-service.ts app/src/server/api/consultation-service.test.ts app/src/server/api/agent-console-admin.test.ts app/src/lib/db/agent-console-repository.ts app/src/lib/db/knowledge-repository.ts app/src/components/platform-admin/agent-console-pages.tsx app/src/components/platform-admin/platform-knowledge-manager.tsx
```

结果：

- 33 条相关测试通过。
- TypeScript typecheck 通过。
- targeted ESLint 通过。
- Next.js production build 通过，50 个页面生成完成。
- diff whitespace 检查通过。
- `node --test` 仍有既有 package ESM warning，不影响本轮验证。

## 追加未做

- 未部署 Vercel。
- 未 commit / push / merge。
- 未做真实线上数据写入验证；当前本地 `.env.local` 仍需按 Production 直连风险处理。

## 追加部署：Supabase + Vercel Production

用户要求推到 Supabase 和 Vercel。

执行结果：

- Supabase linked project：`jingjing-content-platform-staging`，project ref `jrveaabguddromjtibbs`。
- 已执行 `supabase db push --linked --dry-run`：远端数据库 up to date。
- 已执行 `supabase db push --linked --yes`：远端数据库 up to date，无新增 migration 需要推送。
- 已执行 `vercel --prod --yes`，Production 部署成功。

Vercel 部署结果：

- Inspect URL：`https://vercel.com/neveraloofwy-4960s-projects/jingjing-content-platform-staging/44TwqZGsTTTokbjd5vWr8qXFNRr5`
- Deployment URL：`https://jingjing-content-platform-staging-6bmhy601k.vercel.app`
- Production alias：`https://jingjing-content-platform-staging.vercel.app`

线上验证：

```bash
curl -sI https://jingjing-content-platform-staging.vercel.app/
curl -sI https://jingjing-content-platform-staging.vercel.app/login
curl -sI https://jingjing-content-platform-staging.vercel.app/platform-admin-login
```

结果：

- `/` 返回 `HTTP/2 200`
- `/login` 返回 `HTTP/2 200`
- `/platform-admin-login` 返回 `HTTP/2 200`

注意：

- 本次 Vercel CLI 从当前本地未提交工作区直接部署 Production。
- 仍未 commit / git push / merge。
