# 2026-05-10 V2.4 检索优化 staging 部署记录

## 背景

本轮在分支 `codex/rag-material-retrieval-prd` 完成 V2.4 检索优化代码落地后，按用户要求同步检查 Supabase staging 并部署 Vercel staging。

代码实现提交：

- `274e3ea Implement V2.4 retrieval optimization`

## Supabase

目标环境：

- Project：`jingjing-content-platform-staging`
- Project Ref：`jrveaabguddromjtibbs`

执行命令：

```bash
cd app
supabase migration list
supabase db push --dry-run
supabase migration list
```

结果：

- `supabase migration list` 显示本地和远端 migration 已完全一致。
- 当前远端已包含：
  - `202604200001` 到 `202605100002` 的全部本地 migration。
- 本轮 V2.4 检索优化代码没有新增 Supabase migration，因此没有需要 apply 的数据库结构变更。
- `supabase db push --dry-run` 未完成，原因是当前机器没有配置 `SUPABASE_DB_PASSWORD`，CLI 连接临时角色时报 `password authentication failed for user "cli_login_postgres"`。
- 由于 `migration list` 已确认无 pending migration，本轮未执行 `supabase db push`。

结论：

- Supabase staging 迁移状态已确认为最新。
- 本轮没有 Supabase 结构变更需要推送。

## Vercel

目标项目：

- Vercel project：`jingjing-content-platform-staging`
- Alias：`https://jingjing-content-platform-staging.vercel.app`

执行命令：

```bash
cd app
vercel deploy --prod --yes
vercel inspect https://jingjing-content-platform-staging-3p19gu2ue.vercel.app
```

部署结果：

- Deployment URL：`https://jingjing-content-platform-staging-3p19gu2ue.vercel.app`
- Alias：`https://jingjing-content-platform-staging.vercel.app`
- Inspect URL：`https://vercel.com/neveraloofwy-4960s-projects/jingjing-content-platform-staging/3JyXfs1kSEWHQQzhTwMXDRvJpcxE`
- Deployment id：`dpl_3JyXfs1kSEWHQQzhTwMXDRvJpcxE`
- `vercel inspect` 状态：`Ready`

Vercel build 结果：

- 远端依赖安装通过。
- `next build` 编译通过。
- TypeScript 检查通过。
- 54 个静态页面生成通过。
- Deployment completed。
- Alias 已创建到 staging 域名。

## HTTP 验证备注

本地 `curl` 到 Vercel 域名时出现网络 / TLS 层问题：

- alias 请求曾返回 `OpenSSL SSL_connect: SSL_ERROR_SYSCALL`。
- deployment URL 在当前 curl 环境中出现证书主机名不匹配。
- `curl -k` 能建立 HTTP 层请求，但返回的是当前网络链路里的中间错误页，不能作为应用层验收依据。

因此本轮以 Vercel 官方 `inspect` 的 `Ready` 状态和远端 build 日志作为部署成功依据。后续建议在浏览器或正常外网环境中做登录后的图文 / 素材检索 smoke。

## 当前状态

- Supabase：无 pending migration，未执行结构变更。
- Vercel：staging 已部署并 alias 到最新 deployment。
- Git push：未执行。
- Merge：未执行。
