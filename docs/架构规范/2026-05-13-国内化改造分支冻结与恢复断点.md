# 2026-05-13 国内化改造分支冻结与恢复断点

## 1. 文档定位

这是国内化改造的架构级断点文档，用来记录当前 `codex/domestic-infra-migration` 分支已经做到哪里、为什么暂停、后续买好服务器和云资源后怎样恢复验证。

这份文档不是普通 handoff，而是架构变动的恢复入口。后续任何 AI / 开发者接手国内化改造时，都应先读这里，再去读分支内的 runbook 和 progress。

一句话：

```text
国内化代码改造和离线部署准备已经完成到真实资源前；当前不是代码失败，而是因为国内服务器、国内 PostgreSQL、国内 COS、手机端 IP 访问环境尚未采购，所以完整 phase1 e2e 正常阻塞。
```

## 2. 当前冻结点

冻结时间：`2026-05-14`

主项目目录：

```text
/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台
```

国内化 worktree：

```text
/Users/wy/Desktop/静境/静境4.0/jingjing-domestic-infra-migration
```

分支：

```text
codex/domestic-infra-migration
```

冻结 commit：

```text
cdc5ca1 docs: add domestic offline deployment readiness kit
```

上一轮资源阻塞断点：

```text
7e0ef4a docs: mark domestic migration blocked on resources
```

冻结状态：

```text
git status --short：干净
long-task：blocked
是否 complete：否
是否 merge main：否
是否 push：否
是否切 ba-ba-ke.com：否
是否做 ICP 备案动作：否
```

## 3. 为什么暂停

暂停原因不是代码写不下去，而是完整验收所需的真实资源还没有：

- 国内服务器还未购买。
- 国内 PostgreSQL 还未购买。
- 国内腾讯 COS bucket / region / 密钥还未创建。
- 手机端访问服务器 IP 的真实环境还未准备。
- 还没有真实素材上传到国内 COS。
- worker 还没有在国内环境生成真实 `final.mp4`。
- 页面还没有完成对真实 `final.mp4` 的重新签名下载验证。

因此 Completion Gate 缺的是：

```text
DOMESTIC_PHASE1_E2E_PASS
```

这个缺失是正确的。不要为了让 gate 通过而伪造 pass marker。

## 4. 分支已经完成什么

国内化分支已经完成的本地改造方向包括：

- 普通 PostgreSQL baseline。
- Node 侧 PostgreSQL 访问层。
- 最小 domestic session。
- 视频主链路 PostgreSQL repository。
- merchant / media / content-draft / video-edit-job 等主链路 repository 的 PostgreSQL 分支。
- worker 从 `SUPABASE_DB_URL` 过渡为优先 `WORKER_DATABASE_URL`。
- worker 国内 COS 环境变量优先级。
- worker 第一阶段继续强制 `WORKER_MAX_CONCURRENCY=1`。
- heartbeat / timeout / failure_code / worker_id 等任务可靠性字段和逻辑。
- 国内 app env preflight。
- 国内 COS roundtrip preflight。
- video chain API smoke。
- phase1 真实资源 runbook。
- phase1 e2e verification 模板。
- 单机国内部署 readiness kit。
- Nginx / systemd / PM2 / env 模板。
- 资源购买后的 readiness checklist。
- A0-A9 真实进度梳理。
- Supabase / Vercel 依赖审计。

本地验证已覆盖：

- TypeScript typecheck。
- lint。
- Next.js build。
- worker tests。
- worker compileall。
- PostgreSQL baseline / seed。
- 本地 login。
- test draft。
- media complete。
- video job create。
- source item read API。
- mixed Supabase/Postgres env 优先级。
- app env preflight。
- API smoke。
- worker env-file smoke。
- deploy template smoke。
- Docker compose config。
- missing-env 失败路径。

当前 A0-A9 状态：

| 阶段 | 当前状态 |
| --- | --- |
| A0 worktree 隔离 | 已完成 |
| A1 普通 PostgreSQL 访问层 | 第一链路已完成 |
| A2 普通 PostgreSQL baseline | 第一链路已完成 |
| A3 核心 repository 迁移 | 视频主链路已完成 |
| A4 最小 auth / session | owner 登录和 dashboard 路径已完成 |
| A5 国内 COS 配置 | 代码和 preflight 已准备，真实 CORS / STS / 字节上传待资源 |
| A6 worker DB / COS | 配置、测试和 compose surface 已完成，真实 worker 待资源 |
| A7 任务可靠性 | 第一链路代码和测试 surface 已完成，真实超时/重跑证据待资源 |
| A8 国内单机部署 | 离线部署材料已完成，真实安装运行待服务器 |
| A9 端到端验收 | 模板已完成，真实手机 e2e 待资源 |

## 5. 还没有完成什么

以下内容必须等真实资源到位后继续：

| 项 | 当前状态 |
| --- | --- |
| 国内服务器 IP 页面访问 | 未验证 |
| 国内 PostgreSQL 真实连接 | 未验证 |
| 国内 COS CORS / STS / 上传 | 未验证 |
| 手机端浏览器访问 IP | 未验证 |
| 浏览器真实上传视频素材 | 未验证 |
| worker 真实领取任务 | 未验证 |
| FireRed / OpenStoryline 真实生成 `final.mp4` | 未验证 |
| `final.mp4` 上传国内 COS | 未验证 |
| 页面重新签名下载 `final.mp4` | 未验证 |
| `DOMESTIC_PHASE1_E2E_PASS` | 未写入，且不应提前写入 |

## 6. 买好资源后如何恢复

资源到位后，不要从 main 重新开始。直接恢复这个 worktree：

```bash
cd "/Users/wy/Desktop/静境/静境4.0/jingjing-domestic-infra-migration"
git status --short
git branch --show-current
git log -1 --oneline
```

期望看到：

```text
branch：codex/domestic-infra-migration
latest commit：cdc5ca1 docs: add domestic offline deployment readiness kit
status：干净，或只有新的验证记录改动
```

如果 worktree 被删了，可从主项目目录重新拉起：

```bash
cd "/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台"
git worktree add ../jingjing-domestic-infra-migration codex/domestic-infra-migration
cd "../jingjing-domestic-infra-migration"
```

恢复后第一组必读文件：

```text
deploy/domestic/README.md
docs/handoff/2026-05-14-domestic-resource-readiness-checklist.md
docs/handoff/2026-05-13-domestic-phase1-real-resource-runbook.md
```

验证证据写入：

```text
docs/progress/2026-05-13-domestic-migration-phase1-e2e-verification.md
```

## 7. 真实资源准备清单

恢复验证前，需要准备：

```text
1 台国内 8 核 16G 服务器
1 个国内托管 PostgreSQL
1 个同地域国内腾讯 COS bucket
COS CORS 规则
COS_SECRET_ID / COS_SECRET_KEY
用于测试的 owner 邮箱和临时密码
FireRed / OpenStoryline 所需 provider key
手机端国内网络访问环境
```

资源买好后，先按分支内这个 checklist 填参数和跑 preflight：

```text
docs/handoff/2026-05-14-domestic-resource-readiness-checklist.md
```

第一阶段仍然保持：

```text
WORKER_MAX_CONCURRENCY=1
不切 ba-ba-ke.com
不做 ICP 备案动作
不合并 main
不声明生产上线
```

## 8. 恢复后的核心命令

### 8.1 初始化数据库

```bash
psql "$APP_DATABASE_URL" -f app/db/migrations/202605130001_domestic_core_baseline.sql
```

创建最小测试账号：

```bash
HASH="$(node app/scripts/create-domestic-password-hash.mjs '<temporary-password>')"
psql "$APP_DATABASE_URL" \
  -v user_email='owner@example.com' \
  -v password_hash="$HASH" \
  -v display_name='Domestic Test Owner' \
  -v merchant_name='Domestic Test Merchant' \
  -f app/db/seeds/domestic_minimal_seed.example.sql
```

可选：创建视频链路 fixture：

```bash
psql "$APP_DATABASE_URL" \
  -v user_email='owner@example.com' \
  -f app/db/seeds/domestic_video_chain_fixture.example.sql
```

### 8.2 检查 app 环境

```bash
node app/scripts/check-domestic-app-env.mjs \
  --env-file app/.env.production \
  --require-video-chain-test-entrypoint
```

### 8.3 检查 COS

```bash
node app/scripts/check-domestic-cos-roundtrip.mjs \
  --env-file app/.env.production
```

### 8.4 API smoke

```bash
DOMESTIC_SMOKE_EMAIL='owner@example.com' \
DOMESTIC_SMOKE_PASSWORD='<temporary-password>' \
node app/scripts/check-domestic-video-chain-api-smoke.mjs \
  --env-file app/.env.production \
  --base-url "http://<domestic-ip>:<port>" \
  --with-upload-intent
```

注意：API smoke 不能替代真实浏览器上传、worker 生成和 `final.mp4` 下载验证。

### 8.5 worker 环境检查

```bash
PYTHONPATH=workers/video-worker:workers/video-worker/openstoryline \
python -m worker.app.real_io_smoke --env-file workers/video-worker/.env
```

### 8.6 启动 worker

```bash
cd workers/video-worker
docker compose -f docker-compose.yml -f docker-compose.firered.yml --profile firered up -d --build
docker compose -f docker-compose.yml -f docker-compose.firered.yml --profile firered ps
```

## 9. 完整 phase1 验收顺序

必须按真实链路跑：

```text
手机浏览器打开国内 IP 页面
-> 登录 domestic session
-> 创建或打开视频测试草稿
-> 浏览器真实上传视频素材到国内 COS
-> /api/media/complete 写入 asset_objects
-> /api/video-edit-jobs 创建 pending job
-> worker claim job
-> worker 更新 heartbeat / stage
-> FireRed / OpenStoryline 生成视频
-> final.mp4 上传国内 COS
-> video_edit_jobs.succeeded
-> result asset_objects 写入 final.mp4
-> 页面重新签名并下载 / 预览 final.mp4
```

所有证据写入：

```text
docs/progress/2026-05-13-domestic-migration-phase1-e2e-verification.md
```

只有真实完成后，才允许在该文档中写入：

```text
DOMESTIC_PHASE1_E2E_PASS
```

## 10. 合并策略

国内化分支现在不要合并进 main。

等真实资源 e2e 通过后，再开集成分支：

```bash
cd "/Users/wy/Desktop/静境/静境4.0/小红书抖音矩阵获客平台"
git switch main
git switch -c codex/integrate-domestic-infra
git merge --no-ff codex/domestic-infra-migration
```

在 `codex/integrate-domestic-infra` 上重新跑：

```bash
cd app
pnpm exec tsc --noEmit --pretty false
pnpm lint
pnpm build
```

并重新确认 worker 测试和真实 e2e 记录。只有验收通过后，再决定是否合回 `main`。

## 11. 不要做的事

- 不要从 main 重写一遍国内化改造。
- 不要删除 `jingjing-domestic-infra-migration` worktree，除非确认分支已合并或已备份。
- 不要提前写 `DOMESTIC_PHASE1_E2E_PASS`。
- 不要把 API smoke 当成完整 e2e。
- 不要在资源没买好前继续让 long-task 空转。
- 不要切 `ba-ba-ke.com`。
- 不要做 ICP 备案动作。
- 不要声称 8 核 16G 已支持 3 并发。

## 12. 下一位接手者一句话提示

```text
国内化改造分支已经完成本地代码、本地 smoke 和离线单机部署 readiness kit，最新断点是 cdc5ca1。先不要 merge main。买好国内服务器、国内 PostgreSQL、国内 COS 后，切回 codex/domestic-infra-migration，先按 docs/handoff/2026-05-14-domestic-resource-readiness-checklist.md 做资源预检，再按 docs/handoff/2026-05-13-domestic-phase1-real-resource-runbook.md 跑真实 phase1 e2e，并把证据写入 docs/progress/2026-05-13-domestic-migration-phase1-e2e-verification.md。
```

## 13. 资源购买前协作口径

如果资源还没有买好，不要继续让国内化分支空转追 long-task complete。

此时只需要确认：

```bash
cd "/Users/wy/Desktop/静境/静境4.0/jingjing-domestic-infra-migration"
git status --short
git log -1 --oneline
```

期望状态：

```text
git status --short：干净
latest commit：cdc5ca1 docs: add domestic offline deployment readiness kit
project long-task：blocked
```

可以给该分支 Agent 的话术：

```text
先暂停在当前断点，不要继续追真实 e2e，不要标记 project long-task complete。

请确认当前分支 codex/domestic-infra-migration 已提交到 cdc5ca1 docs: add domestic offline deployment readiness kit，git status --short 干净。

当前资源还没买好，所以不要 merge、不要 push、不要切 ba-ba-ke.com、不要做 ICP、不要写 DOMESTIC_PHASE1_E2E_PASS。

等我买好国内服务器、国内 PostgreSQL、国内腾讯 COS 后，我会带着 IP、数据库连接串、COS bucket/region/CORS、测试账号和 provider key 回来。到时候再从 docs/handoff/2026-05-14-domestic-resource-readiness-checklist.md 继续。
```

## 14. 资源购买后恢复提示词

资源买好后，可以直接给接手 Agent 这段提示词：

```text
继续国内化真实资源验证。工作目录：

/Users/wy/Desktop/静境/静境4.0/jingjing-domestic-infra-migration

分支：
codex/domestic-infra-migration

先确认：
git status --short
git log -1 --oneline

期望最新 commit 是：
cdc5ca1 docs: add domestic offline deployment readiness kit

这次不要从 main 重做，不要 merge main，不要 push，不要切 ba-ba-ke.com，不要做 ICP。

我已经买好真实资源，请先按以下文档顺序执行：

1. deploy/domestic/README.md
2. docs/handoff/2026-05-14-domestic-resource-readiness-checklist.md
3. docs/handoff/2026-05-13-domestic-phase1-real-resource-runbook.md
4. docs/progress/2026-05-13-domestic-migration-phase1-e2e-verification.md

目标是先完成真实资源 readiness：

- 国内服务器 IP 可访问 app 和 /api/health
- 国内 PostgreSQL baseline + seed 成功
- 国内腾讯 COS CORS / put / signed GET / delete 成功
- app env preflight 成功
- worker env real_io_smoke 成功
- Docker compose / FireRed / video-worker 能启动

然后再跑真实手机端 phase1 e2e：

手机浏览器打开国内 IP 页面
-> 登录测试账号
-> 创建或打开视频测试草稿
-> 浏览器真实上传视频素材到国内 COS
-> 创建 video_edit_jobs
-> worker claim job
-> FireRed/OpenStoryline 生成 final.mp4
-> final.mp4 上传国内 COS
-> 页面重新签名并预览/下载 final.mp4

所有证据写入：
docs/progress/2026-05-13-domestic-migration-phase1-e2e-verification.md

只有真实完成后，才允许写入：
DOMESTIC_PHASE1_E2E_PASS

如果任一步失败，请记录失败命令、时间、错误、关联 job id / COS key / 日志位置，并停在可恢复状态，不要伪造通过。
```
